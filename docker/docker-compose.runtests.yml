version: '3'

services:
  runtests:
    image: theblusky/teamtasks:backend
    volumes:
      - ../backend/:/tmp_backend/
    links:
      - postgres
    working_dir: /tmp_backend/
    command: ./runtests.sh